#include "main.h"
/**
 * rev_string - reverses a string
 * @s: holi
 *
 */
void rev_string(char *s)
{
	int i;

		while (s[i] != 0)
	{
		i++;
	}
	for (i = i; s[i] != '\0' ; i++)
	{
		_putchar(s[i]);
	}
}
