#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 *  
 * Return: Always 0 (Success)
 **/

int main(void)
{
	char c;
	
	c = getchar()
		if (c == _isupper(c))
		{
		return (1)
		}
    else
		{
			return (0);
		}
}
