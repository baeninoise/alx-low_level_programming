#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main()
{
char txt[] = "_putchar\n";
int i

for (i = 0; i < txt.length(); ++i)
	{
	_putchar(txt[i]);
	}
return (0);
}
