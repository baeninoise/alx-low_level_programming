#include "main.h"
#include "_putchar.c"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

 char txt[] = "_putchar\n";

	for (int i = 0; i < txt.length(); ++i)
	{
	_putchar(txt[i]);
	}
 return (0);
